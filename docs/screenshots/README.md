# Screenshot Assets

Add real product screenshots for recruiter/demo polish:

1. `dashboard-overview.png` — SOC dashboard with KPIs and alert queue.
2. `prediction-result.png` — scored event with confidence + explanation.
3. `recent-alerts.png` — triage table with status and ownership fields.
4. `alert-detail-panel.png` — status transition, assignment, and notes.

Optional GIFs (place in `docs/assets/demo/`):
- `triage-flow.gif`
- `model-observability.gif`

Recommended format:
- PNG 16:9 (min 1280x720)
- GIF <= 20MB for GitHub rendering
- Use realistic but sanitized data
